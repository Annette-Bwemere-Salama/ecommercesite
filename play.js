const name = 'Max';
const age = 29;
const hasHobbies = true;

function summarizeUser(userName, userAge, userHabb) {
    return(
        "Name is" + 
        userName + 'age is' + 
        userAge + 
        userHabb
    );
}

console.logsummarizeUser(name, age, hasHobbies);